'use client';
import { useState } from 'react';
import { buttonVariants } from '@/components/ui/button';
import { cn } from '@/lib/utils';
import { PUBLIC_ANNOUNCEMENT_MODES, PUBLIC_COVER_MODES, PUBLIC_LAYOUT_PRESETS } from '@/src/meetings/public-layout';

export function PublicLayoutClient({ wardId, initial }: { wardId: string; initial: { preset: string; announcement_mode: string; cover_mode: string } }) { const [layout, setLayout] = useState(initial); const [status, setStatus] = useState(''); async function save() { setStatus('Saving…'); const response = await fetch(`/api/w/${wardId}/public-layout`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ preset: layout.preset, announcementMode: layout.announcement_mode, coverMode: layout.cover_mode }) }); const body = await response.json(); setStatus(response.ok ? 'Saved' : body.error ?? 'Save failed'); if (response.ok) setLayout(body.layout); } return <section className="space-y-4 rounded-lg border bg-card p-5 shadow-sm"><h2 className="text-lg font-semibold">Public layout</h2><p className="text-sm text-muted-foreground">Text-first presets. Layout settings do not publish draft program changes automatically.</p><label className="block text-sm font-medium">Preset<select value={layout.preset} onChange={(e) => setLayout({ ...layout, preset: e.target.value })} className="mt-1 block w-full rounded-md border bg-background px-3 py-2">{PUBLIC_LAYOUT_PRESETS.map((preset) => <option key={preset}>{preset.replaceAll('_', ' ')}</option>)}</select></label><label className="block text-sm font-medium">Announcements<select value={layout.announcement_mode} onChange={(e) => setLayout({ ...layout, announcement_mode: e.target.value })} className="mt-1 block w-full rounded-md border bg-background px-3 py-2">{PUBLIC_ANNOUNCEMENT_MODES.map((mode) => <option key={mode}>{mode.replaceAll('_', ' ')}</option>)}</select></label><label className="block text-sm font-medium">Cover<select value={layout.cover_mode} onChange={(e) => setLayout({ ...layout, cover_mode: e.target.value })} className="mt-1 block w-full rounded-md border bg-background px-3 py-2">{PUBLIC_COVER_MODES.map((mode) => <option key={mode}>{mode.replaceAll('_', ' ')}</option>)}</select></label><div className="flex items-center gap-3"><button onClick={save} className={cn(buttonVariants({ size: 'sm' }))}>Save layout</button><span className="text-sm text-muted-foreground" role="status">{status}</span></div></section>; }
