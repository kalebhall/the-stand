export default function SentryExamplePage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-3xl flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="text-2xl font-semibold">Sentry Example Disabled</h1>
      <p className="text-muted-foreground">
        The Sentry wizard example page has been replaced with a safe placeholder to avoid build issues across SDK versions.
      </p>
    </main>
  );
}
